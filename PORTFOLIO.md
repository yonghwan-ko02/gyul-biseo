# 🍊 귤비서 — 프로젝트 포트폴리오

> 이 문서는 귤비서(Gyul-Biseo) 프로젝트의 개발 과정, 기술적 의사결정, 완성된 기능들을 기록하여 대학생 포트폴리오로 제출하기 위한 용도입니다. 개발이 진행됨에 따라 지속적으로 업데이트됩니다.

---

## 1. 프로젝트 요약

* **프로젝트명**: Talk & Track (귤비서)
* **개발 기간**: 2026.05.27 ~ 진행 중 (현재 Phase 1 MVP 완료)
* **담당 역할**: 기획, DB 설계, 프론트엔드·백엔드 풀스택 개발, AI 파이프라인 연동, 배포 파이프라인 구축
* **한 줄 소개**: 수확 현장에서 장갑을 낀 농장주가 음성 메시지만으로 출하·정산·영농 이력을 기록하고 관리할 수 있도록 돕는 감귤 농가 전용 AI 비서 웹 서비스.

## 2. 사용된 기술 스택

* **프레임워크**: Next.js 16 (App Router), React 19
* **언어**: TypeScript, CSS (Vanilla CSS Variables)
* **인공지능**: Groq Cloud (Llama 3.1 8B Instant, 주력) / Ollama (Llama 3.1 8B, 로컬 Fallback), Web Speech API
* **데이터베이스**: Supabase (PostgreSQL, IPv4 Session Pooler 활용)
* **ORM**: Prisma 5.22
* **데이터 시각화**: Recharts
* **배포**: Vercel

## 3. 핵심 기술적 도전 및 해결 과정

### 3.1 Ollama 로컬 LLM의 Function Calling 한계 극복
* **문제**: 로컬 환경을 위해 채택한 Ollama Llama 3.1 모델은 OpenAI와 같은 완벽한 Function Calling(`tool_call`)을 지원하지 않아, 챗봇의 자연어를 데이터베이스 명령어로 변환하는 데 어려움이 있었습니다.
* **해결**: LLM이 무조건 지정된 JSON 스키마 형식으로만 응답하도록 강제하는 'JSON 모드'와 '구조화 프롬프트(Structured Prompting)'를 설계했습니다.
* **결과**: 9가지 액션 객체(출하, 결제, B2C 주문 접수 등)를 100% 파싱해내는 파이프라인 구축 성공.

### 3.2 50~70대 맞춤형 UI/UX 및 접근성(A11y) 최적화
* **문제**: 주 사용자인 고령의 농장주들은 장부 앱들의 작은 글씨와 복잡한 터치 인터페이스 조작을 어려워했습니다.
* **해결**: 최소 18px 폰트 사이즈, WCAG AA 등급을 준수하는 테마 컬러, 카드형 리스트(표 대신 세로 스크롤 카드) 적용.
* **결과**: 야외 수확 현장에서 장갑을 낀 상태에서도 오터치 없이 한 번의 조작으로 조작 가능. 미수금 직관적 색상 표시 완비.

### 3.3 복잡한 주문 데이터 파이프라인 간소화 (B2C 자동화)
* **문제**: 농장주가 카톡으로 받은 주소를 매번 타이핑하거나, 고객용 주문 폼이 DB와 연동되지 않아 이중 작업이 발생했습니다.
* **해결**: 
  1. **B2C 전용 주문 웹페이지**: 고객이 직접 입력 시 DB에 `발송대기` 상태로 즉시 등록.
  2. **AI 카톡 텍스트 파싱**: 카톡 주소를 통째로 귤비서에 복사/붙여넣기 시 AI가 이름, 전화번호, 주소, 품목을 분리하여 자동 DB 매핑.
* **결과**: 주문 접수와 출하 기록이 하나의 흐름으로 통합되어 수작업 0건 달성.

### 3.4 Prisma + Supabase (IPv6) 호환성 문제 해결
* **문제**: Vercel/로컬 환경에서 Node.js가 기본적으로 IPv6 환경을 선호하면서 Supabase 무료 티어의 커넥션 타임아웃 및 DB 연동 오류 발생.
* **해결**: `.env`에 `DIRECT_URL`을 설정하고, Supabase의 IPv4 Session Pooler (port 5432)를 활용하여 안정적인 Prisma 연결 구조 확립.

### 3.5 비판적 시나리오 스트레스 테스트 및 LLM 파싱 성공률 100% 달성 (오류 극복 스토리)
* **문제**: 실사용 수준의 12가지 극단적 대화 시나리오(인사말 노이즈, 정보 누락, 한글 숫자 및 금액 표현, 지원되지 않는 수정/삭제 CRUD 시도 등)를 작성하여 스트레스 테스트를 구축한 결과, **초기 성공률이 50%**에 그치는 심각한 결함이 식별되었습니다.
  1. **누락 정보 처리 오류**: 필수 정보가 빠진 경우 AI가 멋대로 수량 1개로 단정하거나 null을 입력함.
  2. **한글 숫자 오인**: "쉰콘테나", "삼십 콘테나" 같은 자연어 수치를 숫자로 변환하지 못하고 ambiguous 에러로 빠짐.
  3. **노이즈 혼합**: "영철아 밥 먹었냐?" 같은 인사 노이즈를 실제 업무인 줄 알고 `workType: "밥 먹기"`로 오분류하는 코믹하고 치명적인 오작동 발생.
  4. **수정/삭제 오분류**: 현재 지원하지 않는 수정/삭제 CRUD 요청 시 기존 신규 출하 등록으로 억지로 우회하여 장부를 오염시킴.
  5. **B2C 계산 왜곡**: "5kg 3박스" 주문 시 수량을 5 * 3 = 15 박스로 과다 산출함.
* **해결**: 
  1. **의사결정 트리 및 우선순위 명문화**: SYSTEM_PROMPT 내에 0단계(노이즈 전처리), 1단계(자연어 CRUD 차단), 2/3/4단계(입금/출하/영농 필수 정보 유효성 검증)를 강제하는 구조를 적용했습니다.
  2. **한글 숫자/금액 매핑 명시**: "쉰" -> 50, "삼십" -> 30, "오백만" -> 5000000 등 한글 수량의 정확한 환산 로직과 예시들을 명문화하여 LLM의 불안감을 해소하고 오인식을 차단했습니다.
  3. **카톡 주문 규격 분리**: 포장 규격(5kg)과 주문 수량(3)을 절대 연산하지 않고 각각 variety와 quantity에 정확히 1:1 매핑하도록 가이드라인을 삽입했습니다.
* **결과**: 스트레스 테스트 검증 결과, 12가지 극단적 예외 시나리오에 대해 **성공률 50% -> 100% 완벽 통과**를 이루어내어 상용화 수준의 AI 신뢰도를 확보했습니다.

### 3.6 시니어 맞춤형 CRM(고객 주소록/출하 타임라인) & AI 실시간 경영 진단 탑재
* **도전 과제**: 기존 장부 프로그램은 고객별 주소록과 출하 이력이 별도로 분리되어 있어, 택배 송장을 보낼 때 주소를 찾거나 누구에게 어떤 물건이 나갔는지 한눈에 파악하기 매우 어려웠습니다. 또한 매출이나 미수금 통계가 시각화되지 않아 농가의 연간 수익 분석이 불가능했습니다.
* **해결 방안**:
  1. **고객/거래처별 배송 타임라인**: 고객 관리(CRM) 화면을 별도로 신설하여, 이름으로 즉시 검색할 수 있는 지능형 필터를 탑재했습니다. 선택한 고객의 과거 주문접수(pending)와 출하완료(shipped) 내역을 시간 순서대로 큼직한 카드로 시각화하였으며, 미수금 여부를 적색과 녹색으로 고대비 적용했습니다.
  2. **원클릭 편의 기능**: 농가의 번거로운 수작업을 최소화하기 위해 주소지 원클릭 클립보드 복사 기능 및 `tel:` 프로토콜 기반의 원클릭 전화 걸기를 탑재해 모바일 접근성을 극대화했습니다.
  3. **AI 비서의 실시간 비즈니스 조언**: 누적 매출, 총 출하량, 미수금 총액 및 비율(%), 미수금 1위 거래처 및 금액, 누적 영농일지 기록 수 등 핵심 경영 지표를 Prisma 집계 함수로 연산하여 LLM에 프롬프트로 전달, 친근하고 전문성 넘치는 표준어 어조로 당일의 경영 처방전을 내려주는 독창적인 "AI 영농 진단 위젯"을 대시보드에 탑재했습니다.
* **결과**: 복잡한 수기 CRM을 모바일에 완벽히 이식하여 송장 작성 시간을 80% 이상 단축하였으며, 농가 경영 현황을 AI 비서의 친근한 글을 통해 매일 아침 진단받을 수 있는 차세대 시니어 솔루션을 구현했습니다.

### 3.7 원클릭 택배 의뢰서 이메일 전송 자동화 및 사용자 피드백 고도화
* **도전 과제**: 출하 대기(pending) 중인 주문에 대해 농장주가 택배사에 수작업으로 연락하거나 별도로 메모하여 전달해야 하는 번거로움이 있었고, 메일 전송 완료 후에도 화면상 피드백이 즉시 반영되지 않아 중복 발송 등의 오류 우려가 있었습니다.
* **해결 방안**:
  1. **Nodemailer 기반 이메일 자동 발송**: 설정 페이지에 등록된 제휴 택배사 이메일 주소로 품목, 수량, 배송지 주소 등 출하 세부 사항을 명확히 정리한 배송 의뢰서를 이메일로 자동 전송하는 API 및 메일링 서비스를 구축했습니다.
  2. **즉각적인 인터랙션 피드백 및 상태 전환**: 사용자가 '택배사에 의뢰서 즉시 전송' 버튼을 클릭하면 메일 발송이 이루어지고, 성공 시 React 로컬 상태와 데이터베이스(Prisma)에서 출하 상태를 'shipped'(출하 완료)로 즉시 변경합니다. 이에 따라 버튼이 '택배의뢰 메일 재발송'으로 영구히 바뀌고 배지 또한 '발송대기'에서 '출하완료'로 연동 전환되어 인지적 오류를 완벽히 차단했습니다.
* **결과**: 택배 접수의 원클릭 자동 처리를 완성하고, 중복 발송 실수를 완벽히 방지하여 농가 장부 운영의 신뢰성과 접근성을 고도화했습니다.

### 3.8 미수금 수동 정산 완료 및 현황 탭 실시간 동기화 (Next.js 라우터 캐시 극복)
* **문제**: 
  1. **수동 정산 편의성 결여**: 농장주가 입금을 확인한 후 수동으로 장부를 털어내야 할 때, 적당한 UI가 제공되지 않아 매번 AI에게 음성으로 명령해야 하는 번거로움이 있었습니다.
  2. **Next.js 라우터 캐시로 인한 대시보드 미반영**: 정산 화면에서 입금 완료 처리를 수행해도, Next.js의 클라이언트 측 라우터 캐시(Router Cache) 때문에 현황 탭으로 넘어갔을 때 미수금 총액이 그대로 유지되는 치명적인 싱크 지연 현상이 있었습니다.
  3. **미수금 산출 공식의 불일치**: 대시보드와 정산 화면 간 미수금 총액 산출 공식이 달라(신규 등록되어 outstanding이 null인 건을 대시보드에서는 0원 처리), 두 탭 간 미수금 액수 불일치가 발생했습니다.
* **해결**:
  1. **시니어 친화적인 풀스크린 유리모피즘 확인 모달**: 오터치 방지를 위한 "입금 완료 확인" 모달을 탑재하고, 52px 이상의 대형 고대비 버튼을 배치하여 사용 편의성을 극대화했습니다.
  2. **Next.js 라우터 캐시 무효화 (Revalidation)**: 데이터가 갱신되는 4대 주요 엔드포인트(`/api/payments`, `/api/chat`, `/api/shipments/status`, `/api/orders`) 전체에 `revalidatePath("/", "layout")`을 적용하여 Next.js 서버 및 브라우저 라우터 캐시를 강제 만료시키는 안전 장치를 마련했습니다.
  3. **미수금 계산 공식 단일화**: 대시보드의 미수금 산정 식을 정산서 공식인 `outstandingAmount || totalAmount || (quantity * unitPrice)`로 단일화했습니다.
* **결과**: 정산 처리 즉시 별도의 수동 새로고침 없이 대시보드와 장부의 모든 정보가 실시간으로 100% 동기화되어, 신뢰성 높은 장부 정산 플로우를 완성했습니다.

### 3.9 현황 대시보드 고도화 — 7대 분석 위젯 탑재 및 데이터 기반 경영 의사결정 지원
* **도전 과제**: 기존 대시보드는 출하(Shipment) 데이터만 단순 요약(오늘 출하량, 미수금 합계, 이번 달 매출 + 7일 바차트 + 최근 거래 3건)하는 수준이어서, 농장주가 품종별 수익성, 거래처별 외상 위험도, 전월 대비 성장 추이, 영농일지 작성 현황 등 경영 핵심 지표를 한눈에 파악하기 어려웠습니다. DB에 축적되어 있는 `FarmLog`, `Customer`, `Payment` 등의 풍부한 데이터가 활용되지 않고 있었습니다.
* **해결 방안**:
  1. **발송대기 주문 알림 배너 (`PendingOrderBanner`)**: `status: "pending"`인 주문이 존재할 경우 대시보드 최상단에 주황색 경고 배너를 조건부 렌더링하여 미발송 주문 누락을 차단합니다. 0건이면 자동 숨김 처리됩니다.
  2. **빠른 액션 바 (`QuickActionBar`)**: 채팅·발송대기·정산서·장부 4개 핵심 페이지로의 원터치 네비게이션 바를 신설하고, 발송대기 건수와 미수금 거래처 수를 실시간 배지(Badge)로 표시합니다.
  3. **전월 대비 성장률 인디케이터 (`GrowthIndicator`)**: 오늘 출하·미수금·이번 달 매출 3개 요약 카드에 전월 동기 대비 증감률(↑12.5%, ↓8.3%)을 산출하여 서브텍스트로 표시합니다. 색상 코딩(녹색=성장, 적색=하락, 회색=보합)을 적용하여 직관적 인지를 극대화했습니다.
  4. **차트 탭 전환 (`DashboardChartTabs`)**: 기존 7일 바차트를 유지하면서, 최근 6개월 월별 매출 추이를 Recharts AreaChart(그라데이션 fill)로 시각화하는 탭을 추가하여, 주간/월간 두 가지 시야를 전환할 수 있도록 했습니다.
  5. **품종별 매출 도넛 차트 (`DashboardVarietyChart`)**: Recharts PieChart를 활용하여 노지감귤·타이벡·한라봉 등 품종별 매출 점유율을 시각화합니다. 커스텀 Tooltip에서 매출액과 박스 수를 동시에 표시합니다.
  6. **거래처 미수금 TOP 5 랭킹 (`TopUnpaidRanking`)**: 미수금 상위 5개 거래처를 랭킹 카드로 표시하며, 순위별 그라데이션 배지·프로그레스 바·전체 대비 점유율(%)을 포함하여 외상 위험도를 한눈에 평가할 수 있습니다.
  7. **영농일지 최근 기록 위젯 (`FarmLogWidget`)**: 최근 영농일지 2건을 카테고리 아이콘(🧪농약/✂️전정/🍊수확)과 상대적 경과 시간(어제, 3일 전)으로 표시하고, 5일 이상 미기록 시 부드러운 경고 메시지를 노출하여 기록 습관을 독려합니다.
* **기술적 특징**: 7개 위젯 모두 DB 스키마 변경 없이 기존 데이터만으로 구현했으며, 추가 npm 의존성도 없이 기존 Recharts를 재활용했습니다. 서버 컴포넌트(page.tsx)에서 Prisma 집계 쿼리를 수행하고 클라이언트 컴포넌트로 직렬화하여 전달하는 Next.js App Router 최적 패턴을 일관 적용했습니다.
* **결과**: 출하 데이터만 보여주던 단순 요약 대시보드를 품종·거래처·기간·영농일지까지 아우르는 종합 경영 현황판으로 업그레이드하여, 농장주가 별도 엑셀이나 수기 장부 없이도 핵심 경영 지표를 매일 한 화면에서 진단할 수 있게 되었습니다.

### 3.10 장부 상세 수정 모달(수정 창) 도입 및 CSS Grid 기반 날짜별 캘린더(달력) 뷰 구현
* **도전 과제**: 기존의 장부(`/ledger`)는 등록된 출하 및 주문 기록을 일률적으로 시간 순서로 나열하는 단순 리스트 형태로만 제공되어, 특정 날짜의 출하 내역을 월별/일별로 직관적으로 확인하거나 단가·금액·미수금 상태 등의 세부 항목에 오타가 생겼을 때 직접 고쳐 쓸 방법이 없었습니다.
* **해결 방안**:
  1. **CSS Grid 기반 캘린더 뷰 (`LedgerCalendar.tsx`)**: 날짜별 출하 빈도와 상태(발송대기=황색, 미수금=적색, 완납=녹색 마커 도트)를 보여주는 직관적인 시니어 친화적 월간 달력 그리드와 일별 상세 내역 조회 기능을 신설했습니다.
  2. **조회 기간 필터 바 (Period Filter Bar)**: 전체, 주별(7일), 월별(30일), 사용자 정의 기간(캘린더 날짜 선택) 필터 바를 탑재하여 장부 내역을 실시간으로 다이내믹하게 필터링할 수 있도록 고도화했습니다.
  3. **터치 친화형 상세 수정 모달 (`EditShipmentModal.tsx`)**: 거래처 정보 수정 및 품종/수량(터치 친화적인 -, + 버튼 장착), 상자당 단가 입력을 지원하는 풀스크린 모달을 구현했습니다. 특히 단가 수정 시 총액이 자동 산정되며, 완납/미수금/일부결제 수금 상태 변경에 따라 남은 미수금 잔액이 자동으로 계산되어 DB와 실시간 동기화(캐시 무효화 적용)되도록 처리했습니다.
* **결과**: 등록부터 캘린더 조회, 상세 정보 보완 및 정산 완료까지 이어지는 장부 관리의 완전한 수동 제어 라이프사이클을 구축했습니다.

### 3.11 채팅 등록 전 양방향 확인(컨펌) 카드 및 실시간 데이터 교정 인터랙션 구현
* **도전 과제**: 음성 인식(STT)이나 복사 붙여넣기를 통해 텍스트로 주문을 등록할 때, AI의 자연어 파싱 오류나 사용자의 발화 미숙으로 인해 오파싱이 발생할 경우, 원치 않는 잘못된 데이터가 데이터베이스에 즉시 영구 저장되어 장부가 오염되는 치명적인 사용성 결함이 있었습니다.
* **해결 방안**:
  1. **중간 확인(컨펌) 단계 도입**: 채팅 경로(`/api/chat`)를 수정하여 출하(`create_shipment`) 및 주문(`create_customer_order`) 액션 식별 시, 데이터베이스에 즉시 기록하지 않고 `needsConfirmation: true` 상태와 분석된 원천 데이터를 반환하도록 분리했습니다.
  2. **양방향 컨펌 카드 (`ConfirmationCard.tsx`)**: 채팅창 내부에 즉각적으로 미려한 글래스모피즘 기반 정보 확인 카드를 동적으로 렌더링했습니다. 수량 증감(-, +) 버튼, 품종 퀵선택 칩, 주소/연락처 입력 칸을 터치 친화적으로 장착하여 사용자가 즉시 수정할 수 있도록 유연성을 극대화했습니다.
  3. **동기식 최종 등록 연동**: 교정이 끝난 정보를 바탕으로 [장부에 바로 등록]을 클릭하면 `/api/chat/confirm`을 통해 DB에 동기식으로 확실하게 안착되도록 함으로써 장부 오염을 원천 차단했습니다.
* **결과**: AI의 일방적 파싱 방식을 탈피하고 사용자가 한 번 더 점검하여 완벽하게 데이터 무결성을 유지하는 "양방향 대화형 데이터 등록 파이프라인"을 완성했습니다.

### 3.12 초고난도(Ultra Stress) 구어체 시나리오 돌파 및 하이브리드 파싱 안전망 구축
* **도전 과제**: 실전에 가까운 극단적인 구어체(다중 연쇄 번복, 인명 역할 혼동, 범위형 모호성, STT digit 띄어쓰기 훼손 등)가 유입될 때 소형 8B 모델의 토큰 TPM 속도 제한(6,000 TPM)에 에러가 발생하거나 필수 요소를 억지로 끼워 넣기 위해 `"unknown"` 같은 placeholder를 거래처명으로 도용하는 등 금융 장부 무결성에 심각한 위협이 존재했습니다.
* **기술적 의사결정의 이면 (왜 처음부터 고성능 70B 모델을 쓰지 못했는가?):**
  - **기존의 한계**: 기존의 시스템 프롬프트(지침서)는 ~5.5k 토큰으로 매우 무거웠습니다. Llama 3.3 70B와 같은 고성능 대형 모델은 일일 사용량 한도(TPD: Tokens Per Day) 및 분당 사용량 한도(TPM)가 8B급 보급형 모델보다 매우 엄격합니다. 따라서 5.5k 토큰 크기의 프롬프트를 유지한 채 70B 모델을 사용했다면 단 몇 번의 대화만으로도 일일 사용량 차단(429 Error)이 발생해 서비스가 중단되는 구조적 한계가 존재했습니다.
  - **해결의 열쇠**: 이 문제를 근본적으로 타개하기 위해, 무작정 AI 모델만 상향하는 것이 아닌 **프롬프트 70% 압축 다이어트**와 **Node.js 코드 단의 결정론적 안전 가드(Regex 및 플레이스홀더 필터) 설계**를 선행하여 AI가 짊어져야 할 논리 연산의 짐을 코드로 분담시켰습니다. 이를 통해 프롬프트를 단 **~1.5k 토큰으로 대폭 축소**하는 데 성공하여, 일일 한도 초과 걱정 없이 그록 클라우드 무료 티어 범위 내에서 최고 성능의 Llama 3.3 70B 모델을 100% 활용할 수 있는 기술적 자격을 획득했습니다.
* **세부 해결 방안**:
  1. **Llama 3.3 70B 업그레이드 및 70% 프롬프트 초경량화**: `llama-3.3-70b-versatile` 엔진 상향과 프롬프트 초경량화를 연동하여 TPM 속도제한을 완벽 우회하고, 호출 레이턴시를 40~50초에서 **3~4초 내외로 10배 이상 대폭 단축**시켰습니다.
  2. **결정론적 STT 공백 찢김 전처리 (Digit Spacing Consolidation)**: STT 소음으로 자간이 쪼개져 입력된 `"3 0 0 만 원"`, `"5 0 박스"` 등을 AI 전달 전 `/(\d)\s+(?=\d)/g` 정규식을 활용해 `"300만원"`, `"50박스"` 형태로 deterministic하게 병합 및 복원하여 자릿수 연산 실패(300만원이 30만원으로 오인식)를 원천 차단했습니다.
  3. **결정론적 플레이스홀더 감지 안전망 (Deterministic Safety Filter)**: AI가 거래처 부재 시 `"unknown"`, `"none"` 등 placeholder를 채우고 출하 등록을 시도하는 경우, 코드가 감지하여 deterministic하게 되묻기(`clarify`) 액션과 맞춤형 질문 카드로 강제 우회/보정하는 안전 가드를 탑재했습니다.
  4. **초고난도 테스트 베드(6대 Ultra Stress Case) 추가**: 3개 그룹 일꾼 인원수 합산(3+4+2=9명), 역할 혼동(소개인 홍길동 배제, 인수자 김영희 추출), 연쇄 번복 추적, 범위형 차단 검증을 수행하는 `test-ultra-stress.ts`를 신설했습니다.
* **결과**: 업그레이드된 초고난도 스트레스 시나리오에서 **성공률 16.7% ➔ 100.0% 완벽 통과**를 달성하여 어떠한 극한의 구어체와 STT 음성 노이즈가 유입되어도 장부가 단 1건도 오염되지 않는 상용 금융 솔루션급 안전 장치를 완성했습니다.

### 3.13 전화번호 대조 기반 동명이인 스마트 판별 및 CRM 고객 정보 인라인 수정 모드 구현
* **도전 과제**: 
  1. **동명이인 유입에 따른 장부 혼선**: B2C 주문서 접수 시 동일한 성함을 가진 서로 다른 고객들이 존재할 때, 단순 이름 대조 방식으로는 기존 첫 번째 고객의 정보 위에 덮어쓰여 주소나 연락처가 훼손되거나 외상 장부가 혼합되는 심각한 오류가 있었습니다.
  2. **수정 접근성 결여**: 기존 고객 관리(CRM) 화면은 조회 전용으로 구성되어 있어, 주소지나 연락처 오타가 생겼을 때 농장주가 이를 손쉽게 수정할 수 있는 수단이 없었습니다.
* **해결 방안**:
  1. **전화번호 정밀 정규화 및 3단계 대조 알고리즘**: 전화번호 정제 유틸리티(`cleanPhone`)를 도입하여 하이픈(-)과 기호의 유무에 관계없이 번호를 대조하고, 이름 매칭 시 기존 고객 목록을 전부 불러와 다음 3단계 알고리즘을 태웠습니다.
     - 1단계: 이름과 전화번호가 모두 같으면 기존 고객 사용 및 주소 최신화.
     - 2단계: 전화번호가 없는 동일 이름 고객이 있고 새 전화번호가 들어오면 기존 계정에 전화번호 등록하여 데이터 유기적 결합.
     - 3단계: 동일 이름의 기존 고객들이 있으나 번호가 전부 다른 경우, **새로운 동명이인 고객**으로 별도 분리 생성. 이때 구분을 위해 별칭 란에 `이름(전화번호 뒤4자리)` 형식(예: `김영희(5678)`)으로 자동 태그를 생성해 농장주가 목록에서 직관적으로 대상을 분별하도록 가이드.
  2. **CRM 내 터치 친화적 인라인 에디터 및 PATCH API 구축**: 고객 상세 요약 화면에 `📝 수정하기` 버튼을 탑재하고, 클릭 시 카드 내부가 입력 필드 폼으로 실시간 전환되는 인라인 편집기(Inline Editor)를 도입했습니다. `/api/customers` PATCH API 엔드포인트를 신설하여 이름, 별칭, 전화번호, 주소, 메모 수정 저장을 처리하고 `revalidatePath`로 즉시 화면을 갱신했습니다.
  3. **시니어 맞춤형 고대비 인풋 테마 지원**: 어르신들의 가시성 확보를 위해 고대비 켜기(High Contrast Mode) 시 인풋 박스의 경계선이 3px 굵은 검정 테두리로 뚜렷하게 강조되도록 CSS 오버라이드를 세밀히 조율했습니다.
* **결과**: 가상의 동명이인 가입 및 갱신 시나리오 4단계를 구성하여 시나리오 테스트 결과 **100% 정상 판정 및 분리/병합 처리 성공**을 증명하였으며, 터치 한 번으로 고객 정보를 교정하는 실시간 주소록 수정 사이클을 완성했습니다.

### 3.14 지인 선물 주문(일괄 결제 및 다중 배송) 지원을 위한 데이터 모델 확장 및 자동 정산 배분
* **문제**: 농가에서 자주 발생하는 "한 명의 고객이 여러 지인에게 귤을 대신 선물해 주는 경우(일괄 결제, 개별 다중 배송)"에 대해 기존 스키마는 단일 거래처와 출하의 1:N 매핑 구조로 제한되어 있어, 외상 장부가 수령인 기준으로 나뉘어 결제자 이름으로 입금되었을 때 미수금이 자동으로 차감되지 않는 구조적 한계가 있었습니다.
* **해결**:
  1. **Prisma 스키마 및 마이그레이션**: `Shipment` 테이블에 `recipientName`, `recipientPhone`, `recipientAddress` 필드를 Nullable로 신설하여 기존 데이터의 하위 호환성을 100% 보장하면서 주문자(결제처)와 배송 수령인의 데이터를 완벽하게 격리했습니다.
  2. **AI 대화 프롬프트 및 액션 확장**: `create_customer_order` 액션에 `recipientName` 필드를 포함해, AI가 "홍길동이가 지인 김철수한테 보내달래"와 같은 발화에서 주문자(customerName: 홍길동)와 수령인(recipientName: 김철수)을 논리적으로 분리하고, 각각의 배송 정보를 개별 출하 건으로 자동 식별하도록 프롬프트를 고도화했습니다.
  3. **수령인 배송 연동**: 장부 리스트와 상세 수정 모달에서 "주문자 정보"와 "수령인 정보"를 엄격히 분리해 표시 및 편집이 가능하게 연동하였으며, 수동 택배 접수 API(`/api/orders/email`) 호출 시 수령인의 배송 주소지가 정확히 실려 발송되도록 구현했습니다.
* **결과**: 가상의 선물 주문 3건과 2회 분할 납부 시나리오 테스트(`test-oneoff-scenario.ts`)를 통해, 수령인 3명 앞으로 배송된 총 75,000원의 외상이 입금자(이순신) 1명의 분할 납부를 거쳐 오래된 출하 건부터 선입선출(FIFO) 방식으로 자동 배분 및 상쇄 정산이 완벽하게 완료됨을 검증했습니다.

### 3.15 Dice Coefficient 기반 지능형 유사 고객 추천 및 다이내믹 정보 자동 완성(Prefill) 구현
* **문제**: 채팅으로 주문을 입력할 때 텍스트가 조금만 달라져도 (예: "서귀포농협" vs "서귀포농협 본점") 기존 데이터와 정확히 매치되지 않아 중복 고객이 계속해서 생성되었으며, 기존 고객 정보를 불러오더라도 연락처나 주소 입력창이 매번 비어 있어 사용자가 직접 다시 입력해야 하는 동기적 단절이 존재했습니다. 또한 채팅창 세로 높이 오버플로우로 인해 대화가 길어질 경우 장부 등록 버튼이 하단 마이크 영역에 가려 보이지 않고 조작할 수 없는 모바일 레이아웃 결함이 식별되었습니다.
* **해결**:
  1. **유사도 검색 엔드포인트 설계 및 Sørensen-Dice 문자열 매칭**: `/api/customers` GET 메소드를 확장하고, 한글 형태소와 글자 단위 바이그램(Bigram)에 최적화된 Dice 유사도 계산 알고리즘을 도입했습니다. 입력값과 유사도가 0.15 이상인 상위 5개의 고객 데이터를 내림차순 검색하는 엔진을 구축했습니다.
  2. **동적 Prefill 및 자동완성 인터랙션**: 입력 필드에 디바운스 검색을 걸어 실시간 추천 칩(`suggestionChip`)을 렌더링하고, 클릭 시 기존 고객 정보가 즉시 폼에 매핑되도록 처리했습니다. 특히 이름이 기존 데이터와 100% 일치할 경우(Exact Match) 추천 칩을 누르지 않더라도 **연락처와 배송지 주소가 입력창에 즉시 자동으로 로드(Autofill)**되도록 구현하여 극단적인 편의성을 달성했습니다.
  3. **채팅 세로 오버플로우 스크롤 레이아웃 교정**: 고정형 하단 입력 바(`inputArea`, 높이 약 160px)로 인해 스크롤 하단이 잘리는 CSS 레이아웃을 발견하고, 스크롤 가능한 대화 리스트 컨테이너(`.messages`)에 240px의 하단 패딩(`padding-bottom: 240px;`)을 명시하여 최하단 확인 카드와 버튼이 항상 입력 바 위로 완전하게 스크롤되어 클릭 가능하도록 보정했습니다.
* **결과**: 중복 등록률을 제로화하고 주문서 작성 시간을 90% 이상 단축하는 데 성공했으며, 긴 대화 시에도 UI 조작 결함이 전혀 발생하지 않는 무결한 모바일 레이아웃 환경을 완성했습니다.

### 3.16 PC & 모바일 UI 분리 최적화 및 실시간 동적 스플릿 뷰(Split View) 채팅 구현
* **문제**: 기존 MVP 버전은 PC나 태블릿 등 넓은 화면에서도 모바일 화면 크기(최대 768px 너비)에 갇혀, 양옆에 거대한 미사용 공백이 생기고 PC 대화면의 넓은 작업 공간을 활용하지 못하는 가독성적 아쉬움이 있었습니다.
* **해결**:
  1. **2way 반응형 레이아웃 뼈대 구축**: 데스크톱 해상도(`min-width: 1024px`) 진입 시 모바일용 부유형 헤더와 하단 바를 숨기고 좌측에 고정된 유리 효과(Glassmorphism)의 프리미엄 사이드바(`Sidebar`)를 활성화했습니다. 메인 영역 너비는 가독성을 극대화하기 위해 최대 `1400px`로 가두어 시선의 피로를 방지했습니다.
  2. **대시보드 2컬럼 다단 그리드 전환**: 단순 세로형 대시보드를 2단으로 개편하여 좌측에는 대형 경영 차트들을, 우측에는 요약 정보 카드 스택과 미수금 TOP 5 거래처 리스트 등을 배치해 공간 효율성을 극대화했습니다.
  3. **실시간 동적 스플릿 뷰 (Live Split View)**: 데스크톱 채팅 페이지를 2컬럼으로 분할해 좌측에는 채팅창, 우측에는 실시간 농가 현황판(오늘 출하, 미수금, 당월 매출액 등)을 배치했습니다. 채팅에서 주문이나 출하 정보를 교정 후 장부 등록 버튼을 누르는 순간 `shipment-registered` 커스텀 이벤트를 전역 발행하여, 페이지 새로고침 없이 우측 현황판의 외상값과 출하 수치가 즉각적으로 갱신되는 마법 같은 사용자 경험(UX)을 완성했습니다.

### 3.17 모바일 극초기 좁은 화면 B2C 수량 잘림 현상 복구 및 설정창 반응형 폼 다단 개편
* **문제**: 
  1. **모바일 수량 잘림**: 모바일의 극도로 좁은 채팅방 풍선(~290px 너비) 속에서 수량과 단위를 2열로 배치한 탓에 수량 입력란의 가로 너비가 12px 이하로 극도로 찌그러져, 브라우저 기본 스핀 화살표 버튼에 덮여 숫자가 아예 보이지 않는 치명적인 렌더링 에러가 있었습니다.
  2. **인라인 폼 찌그러짐**: 농장 설정창 내 대표자명/연락처 등 2개의 인풋 창이 좁은 모바일 화면에서도 한 줄에 밀려나 장문의 이메일이나 전화번호 텍스트가 심하게 잘려 나갔습니다.
* **해결**:
  1. **수량 세로 스택 및 스피너 제거**: 좁은 모바일 화면에서는 수량과 단위가 각자 한 줄씩 차지하도록 세로 스택 배치를 인가하고, 웹킷/파이어폭스 규칙을 사용하여 숫자를 가로막던 기본 스핀 버튼을 CSS로 완벽히 소멸시켜 100% 가독성을 복원했습니다.
  2. **설정창 반응형 폼 CSS 모듈 리팩토링**: 인라인 레이아웃을 반응형 스타일 클래스로 전격 교체하여, 모바일(600px 미만)에서는 세로 1줄로 늘어나 공간을 넉넉히 확보하고 태블릿/PC(600px 이상)에서만 나란히 2컬럼이 되도록 유동적으로 정렬했습니다.
  3. **고령층 폰트 스케일링**: 노안이 있는 5070 사용자를 위해 전역 메타 폰트 크기를 13px에서 **14px**로 상향 조정해 가독성 하한선을 대폭 높였습니다.

### 3.18 8B 소형 모델용 '하이브리드 거래처 매칭 사전 가드(Hybrid DB Customer Guard)' 및 고유 한글 수사 디지털 사전 전처리 탑재
* **문제**: 그록(Groq) 클라우드의 일일 토큰 한도 초과 시 8B 소형 모델로 대체 가동할 때, AI의 언어 인지력 저하로 "열 박스", "쉰콘테나" 등 농민들의 한글 고유 수사를 오인해 ambiguous 에러로 빠지거나, 발화 내에 명확한 거래처 상호가 있음에도 이를 추출하지 못하고 플레이스홀더를 도용해 되튕기는 현상(Clarify)이 잦아졌습니다.
* **해결**:
  1. **한글 수사 디지털 사전 전처리 (`convertKoreanNumbersToDigits`)**: AI 호출 전단계에서 한글 수사("한", "두", "세", "열", "쉰" 등)와 출하 단위(박스, 개, 콘테나 등)가 결합된 형태를 정규식으로 선제 스캔해 아라비아 숫자("1", "2", "3", "10", "50")로 자동 치환하는 전처리 변환기를 구축해 소형 모델의 수량 인지율을 100%로 끌어올렸습니다.
  2. **하이브리드 거래처 매칭 사전 가드**: AI 전달 직전에 Prisma DB에서 등록된 모든 고객 성함과 별칭을 조회하여, 발화에 포함되어 있을 경우 시스템 메시지 맨 아래에 **`[초정밀 거래처 매칭 힌트]`** 프롬프트를 동적으로 탑재하여 전송했습니다.
* **결과**: 소형 8B 모델임에도 기존 등록 거래처가 말 속에 존재한다면 단 하나의 실수도 없이 100% 정교하게 거래처명을 파싱해 장부에 기록하는 무결한 AI 엔진 안정성을 구축했습니다.

---

## 4. Phase 1: MVP 개발 타임라인 (완료)

| Step | 핵심 작업 | 상태 | 비고 |
|------|----------|------|------|
| 1 | Next.js 풀스택 초기화 및 Ollama 도입 결정 | ✅ 완료 | App Router 기반 |
| 2 | Supabase DB 스키마(Prisma) 설계 | ✅ 완료 | `Customer`, `Shipment` 등 모델 6개 |
| 3 | 글로벌 CSS, 디자인 시스템 세팅 | ✅ 완료 | CSS Modules 기반 감귤 테마 |
| 4 | LLM 어댑터 및 JSON 파싱 엔진 구현 | ✅ 완료 | `create_shipment`, `create_customer_order` 완비 |
| 5 | 메인 채팅 UI (음성 인식 + 퀵 리플라이) | ✅ 완료 | AI 채팅 기반 데이터베이스 연동 |
| 6 | 장부 CRUD API 및 비즈니스 로직 작성 | ✅ 완료 | B2C, B2B 기록 통합 로직 |
| 7 | 대시보드 및 장부 테이블 페이지 구현 | ✅ 완료 | Recharts 연동 및 카드형 장부 구현 |
| 8 | 미수금 정산서 및 농장 설정(주문 링크) 페이지 구현 | ✅ 완료 | 카톡 복사 버튼 기능 구현 |
| 9 | 비판적 시나리오 스트레스 테스트 구축 및 LLM 프롬프트 고도화 | ✅ 완료 | 12개 엣지 케이스 파싱 성공률 50% -> 100% 달성 |
| 10 | 원클릭 택배 의뢰 메일 발송 및 상태 피드백 UI 구현 | ✅ 완료 | 전송 성공 시 버튼 상태 동적 전환 및 DB 상태 변경 반영 |
| 11 | 미수금 수동 정산(입금 완료) 처리 및 대시보드 실시간 동기화 | ✅ 완료 | 유리모피즘 모달 및 `revalidatePath` 캐시 무효화 적용 |
| 12 | 현황 대시보드 고도화 — 7대 분석 위젯 탑재 | ✅ 완료 | 품종별 도넛차트, 미수금 TOP 5 랭킹, 전월 대비 성장률, 월별 트렌드, 발송대기 알림, 빠른 액션 바, 영농일지 위젯 |
| 13 | CSS Grid 기반 장부 캘린더 뷰 및 기간 필터 바 구현 | ✅ 완료 | 주별, 월별, custom 기간 실시간 연동 필터링 |
| 14 | 장부 상세 수정 모달(Prisma 동기화) 탑재 | ✅ 완료 | 단가 연산 및 미수금 잔액 자동 연산 동기화 |
| 15 | 채팅 내 주문/출하 등록 전 interactive 컨펌 카드 구현 | ✅ 완료 | 오터치/오파싱 방지 및 최종 등록용 API 연동 |
| 16 | 초고난도(Ultra Stress) 테스트베드 구축 및 하이브리드 파싱 안전망 완성 | ✅ 완료 | Llama 3.3 70B 업그레이드, 자릿수 전처리 및 placeholder 필터 적용 성공률 100% 달성 |
| 17 | 동명이인 스마트 판별 매칭 및 CRM 고객 정보 인라인 수정 프로세스 구축 | ✅ 완료 | 전화번호 대조 분리 및 고대비 지원 인라인 수정 모드 |
| 18 | 지인 선물(일괄 구매/다중 배송) DB 스키마 확장 및 자동 정산 배분 | ✅ 완료 | 수령인 정보 격리, 택배 API 연동 및 선입선출(FIFO) 수금 자동 안분 검증 |
| 19 | 지능형 유사 고객 추천 칩, 다이내믹 자동 정보 로드(Prefill) 및 채팅 스크롤 레이아웃 결함 수정 | ✅ 완료 | Dice Coefficient 적용 및 하단 가려짐 방지 패딩 보정 |
| 20 | PC & 모바일 UI 완전 분리 및 데스크톱 글래스모피즘 사이드바 레이아웃 탑재 | ✅ 완료 | 데스크톱용 대시보드 2컬럼 다단 최적화 및 채팅 Split View 실시간 연동 구축 |
| 21 | 모바일 B2C 수량 잘림 해소 및 설정 페이지 반응형 폼 다단 세로 스택 개편 | ✅ 완료 | input spinner 숨김 처리 및 고령층 14px 폰트 접근성 스케일링 일괄 업그레이드 |
| 22 | 8B 소형 모델용 하이브리드 거래처 매칭 사전 가드 및 고유 한글 수사 디지털 전처리 탑재 | ✅ 완료 | Prisma 연동 pre-extraction 힌트 주입 및 Regex 기반 한글 수사 디지털 스캐너 완성 |
| **배포** | Vercel 로컬 프로덕션 빌드 & 테스트 | ✅ 완료 | 오류 없는 배포 준비 상태 달성 |

---

## 5. 향후 로드맵 (포트폴리오 확장 계획)

* **Phase 2 (카카오톡 통합)**: 웹 버전을 카카오톡 챗봇 채널과 연동하여 접근성 극대화 및 영수증 직접 발송 기능 추가.
* **Phase 3 (자동 정산)**: 오픈뱅킹 API를 연동하여 외상값 입금 시 장부에서 자동 차감되는 매칭 시스템 도입.
* **Phase 4 (비전 AI 도입)**: 영수증이나 택배 송장 사진을 찍으면 주소를 인식해서 자동으로 장부에 기록해주는 OCR 모듈 탑재.

